name=hello
from_source=hello
revision=1
hostdeps="gcc automake pkg-config"
deps="mlibc"

build() {
	cp -rp "${source_dir}"/. ./
	sed -i "1s/.*/CC = ${OS_TRIPLET}-gcc/" Makefile
	make -j${parallelism}
}

package() {
	mkdir -p "${dest_dir}/usr/bin"
	cp ./hello "${dest_dir}/usr/bin"
}
