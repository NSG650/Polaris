name=libiconv
from_source=libiconv
revision=1
hostdeps="gcc autoconf automake libtool pkg-config"
deps="mlibc-base libgcc libstdc++"
imagedeps="binutils"

configure() {
    ${source_dir}/configure           \
        --host=x86_64-polaris         \
        --prefix=${prefix}            \
        --disable-static              \
        --enable-shared               \
        --disable-nls
}

build() {
    make -j${parallelism}
}

install() {
    DESTDIR="${dest_dir}" make install-strip
}
